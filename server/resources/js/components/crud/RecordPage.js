import React from 'react';
import Container from '../Container';

const RecordPage = () => {

    const {table, recordId} = useParams();

    return (
        <Container>

        </Container>
    );
};

export default RecordPage;